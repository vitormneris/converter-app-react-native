import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop: 30,
        marginBottom: 50,
    },

    title: {
        color: '#000', //'#FF0043',
        fontSize: 20,
        fontFamily: 'google-sans',
        fontWeight: '900'
    }
})